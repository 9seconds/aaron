#!/bin/sh

OUTPUT_DIR="$(readlink -m "$1")"
mkdir -p "$OUTPUT_DIR" || true

cd ./feeder

rm -f "$OUTPUT_DIR/$2.xml.gz"
poetry run scrapy crawl -O "$OUTPUT_DIR/$2.xml:atom" "$2"
zopfli --i1000 "$OUTPUT_DIR/$2.xml"
